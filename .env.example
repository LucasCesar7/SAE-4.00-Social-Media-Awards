# Copy this file to .env (project root) and fill values.
# The app also supports config/.env for backward compatibility.

APP_ENV=development

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=social_media_awards
DB_USER=root
DB_PASS=change_me
DB_CHARSET=utf8mb4
DB_CONNECT_TIMEOUT=2
